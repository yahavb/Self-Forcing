apiVersion: batch/v1
kind: Job
metadata:
  name: self-forcing
  namespace: default
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: self-forcing
    spec:
      restartPolicy: Never
      nodeSelector:
        node-type: trn2
      resourceClaims:
        # trn2, matching rolling_forcing/rolling-forcing-job.yaml. trn3-dev1 was at 95%
        # memory and could not fit this alongside its neighbours. To go back: node-type
        # trn3-dev1 + claim m-lnc1 + NPROC 16 + LATENT_W 80 + the fs1200 config.
        - name: m-trn2
          resourceClaimTemplateName: m-trn2
      containers:
        - name: inference
          image: 421672808698.dkr.ecr.us-east-1.amazonaws.com/concourse-release-0461d3b:latest
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - "-exc"
            - |
              set -euxo pipefail

              SF_BRANCH="${SF_BRANCH:-neuron-trn}"
              # rolling_forcing supplies the Neuron stack (models/, kernels/, utils/).
              # Pinned: models/kernels/utils/e2e_pipeline.py at this commit are what the
              # port was written against (identical across origin/main and rf-distill-1.3b).
              RF_COMMIT="${RF_COMMIT:-0cfc734656aa0ff72438f4ba2771056f4df52807}"

              cd /tmp
              # set +x around the clones: with -x the expanded URL puts GITHUB_TOKEN in
              # plaintext into the pod log, where anyone with `kubectl logs` can read it.
              set +x
              git clone -q -b "$SF_BRANCH" "https://yahavb:${GITHUB_TOKEN}@github.com/yahavb/Self-Forcing.git"
              git clone -q "https://yahavb:${GITHUB_TOKEN}@github.com/yahavb/rolling_forcing.git"
              # Drop the credential-bearing remote URLs so later git calls can't echo them.
              git -C Self-Forcing remote set-url origin https://github.com/yahavb/Self-Forcing.git
              git -C rolling_forcing remote set-url origin https://github.com/yahavb/rolling_forcing.git
              set -x
              echo "Cloned Self-Forcing ($SF_BRANCH) and rolling_forcing."
              git -C rolling_forcing checkout --detach "$RF_COMMIT"

              echo "┌── PROVENANCE ──"
              echo "│ Self-Forcing : $(git -C Self-Forcing rev-parse --abbrev-ref HEAD) @ $(git -C Self-Forcing rev-parse HEAD)"
              echo "│                $(git -C Self-Forcing log -1 --pretty=%s)"
              echo "│ neuron stack : rolling_forcing @ $(git -C rolling_forcing rev-parse HEAD)"
              echo "└────────────────"

              # ─── ENVIRONMENT: record it, because it moves ────────────────
              # The identical job config produced a good video once and then failed to
              # compile with the same SBUF error at the same frame count. The repo was
              # not the variable, so the environment must be. Two suspects: the image is
              # tagged :latest (a node that pulls fresh gets a different neuronx-cc), and
              # the logical-NC config (at LNC=2 the per-partition SBUF budget halves, so
              # an allocation that fit at LNC=1 does not). Print both every run.
              echo "┌── ENVIRONMENT ──"
              echo "│ node        : ${NODE_NAME:-unknown}"
              echo "│ neuronx-cc  : $(neuronx-cc --version 2>&1 | head -1)"
              echo "│ torch-nx    : $(python3 -c 'import torch_neuronx;print(torch_neuronx.__version__)' 2>&1 | tail -1)"
              echo "│ torch       : $(python3 -c 'import torch;print(torch.__version__)' 2>&1 | tail -1)"
              echo "└────────────────"
              echo "--- neuron-ls (device/core topology, shows LNC) ---"
              neuron-ls 2>&1 | head -20 || echo "(neuron-ls unavailable)"
              echo "--- NEURON_RT / LNC env ---"
              env | grep -E 'NEURON_RT|LOGICAL_NC|NEURON_VISIBLE' | sort || true

              cd /tmp/Self-Forcing
              export PYTHONPATH=/tmp/rolling_forcing

              # ─── Install deps (same pinned set as rolling_forcing) ────────
              # This DLC image does not auto-activate a venv, so a bare
              # `uv pip install` dies with "No virtual environment found".
              # Target the same interpreter python3 uses (where torch-neuronx
              # already lives) via --python, as rolling_forcing/rf-job.yaml does.
              # torchvision is deliberately NOT installed: nothing on the path we
              # use needs it (utils/video.py writes mp4 through av).
              UVPY="$(command -v python3)"
              echo "uv target interpreter: $UVPY"

              uv pip install --python "$UVPY" \
                "diffusers==0.37.1" \
                "transformers==5.8.1" \
                "huggingface-hub==1.16.4" \
                "click" \
                "einops==0.8.2" \
                "omegaconf==2.3.0" \
                "ftfy==6.3.1" \
                "regex==2026.5.9" \
                "av==17.0.1" \
                "Pillow"

              # ─── Schedule gate: cheap, catches a bad port before the device ──
              python3 neuron/tests/test_schedule.py

              # ─── Model weights: Wan2.1-T2V-1.3B (T5 + VAE) ────────────────
              # Shared with rolling_forcing — same backbone, same tar. /var/mdl is
              # S3-backed, so one tar beats per-file reads. The trn3-dev1 jobs cache
              # under /var/mdl/wan_models/, the trn2 job under /var/mdl/rolling_forcing/;
              # take whichever exists, and write the trn3 path when neither does.
              WAN_TAR="/var/mdl/wan_models/Wan2.1-T2V-1.3B.tar"
              for cand in /var/mdl/wan_models/Wan2.1-T2V-1.3B.tar \
                          /var/mdl/rolling_forcing/Wan2.1-T2V-1.3B.tar; do
                if [[ -f "$cand" ]]; then WAN_TAR="$cand"; break; fi
              done

              mkdir -p wan_models
              if [[ -f "$WAN_TAR" ]]; then
                echo "Extracting Wan2.1-T2V-1.3B from PVC cache ($WAN_TAR)..."
                tar xf "$WAN_TAR" -C wan_models/
              else
                echo "Downloading Wan2.1-T2V-1.3B from HuggingFace..."
                python3 -c "
              from huggingface_hub import snapshot_download
              snapshot_download('Wan-AI/Wan2.1-T2V-1.3B', local_dir='wan_models/Wan2.1-T2V-1.3B', local_dir_use_symlinks=False)
              "
                echo "Caching to PVC..."
                tar cf /tmp/Wan2.1-T2V-1.3B.tar -C wan_models Wan2.1-T2V-1.3B
                mkdir -p "$(dirname $WAN_TAR)"
                cp /tmp/Wan2.1-T2V-1.3B.tar "$WAN_TAR"
                rm -f /tmp/Wan2.1-T2V-1.3B.tar
              fi

              # ─── Model weights: Self-Forcing DMD checkpoint ───────────────
              # Single file, so cached plain (no tar) under the same /var/mdl/checkpoints/
              # the trn3-dev1 RF jobs use.
              SF_CACHE="/var/mdl/checkpoints/self_forcing_dmd.pt"
              mkdir -p checkpoints

              if [[ -f "$SF_CACHE" ]]; then
                echo "Copying Self-Forcing DMD checkpoint from PVC cache..."
                cp "$SF_CACHE" checkpoints/self_forcing_dmd.pt
              else
                echo "Downloading Self-Forcing DMD checkpoint from HuggingFace..."
                python3 -c "from huggingface_hub import hf_hub_download; hf_hub_download('gdhe17/Self-Forcing', 'checkpoints/self_forcing_dmd.pt', local_dir='.')"
                echo "Caching to PVC..."
                mkdir -p "$(dirname $SF_CACHE)"
                cp checkpoints/self_forcing_dmd.pt "$SF_CACHE"
              fi

              ls -la wan_models/Wan2.1-T2V-1.3B/ checkpoints/
              echo "Model weights ready."

              # ─── Mirror rolling_forcing/rf-job.yaml exactly ─────────────
              # rf-job.yaml runs on this same node-type, this same m-lnc1 claim and this
              # same image, with: --nproc_per_node=16, TP_DEGREE=4, LATENT_W=80,
              # CONFIG_PATH=...fs1200. That is the configuration that works here.
              #
              # I originally picked 8 ranks at latent_w=104 (frame_seq_length 1560)
              # because 1560 is not divisible by 16, and then spent five runs fighting
              # the consequences. The VAE is what pays for it -- bytes per SBUF partition
              # for one frame at the decoder's last stage:
              #   RF: 16 ranks, w=80   ->  40 px/rank ->  38400
              #   me: 8 ranks,  w=104  -> 104 px/rank ->  99840
              #   me: unsharded w=104  -> 832 px/rank -> 798720  <- what the compiler
              #                                                     rejected, every run
              # 1200 IS divisible by 16, which is exactly why rolling_forcing uses it.
              #
              # Do not deviate from these four values without a reason better than the
              # one I had.
              #
              # ─── Run: ${NPROC} ranks, TP=4 ──────────────────────────────────
              # NPROC=16 mirrors rolling_forcing (SP=4). NPROC=8 (SP=2) is the overhead
              # experiment: at SP=4 each rank holds only 3*1200/4 = 900 query tokens per
              # forward, small enough that fixed per-dispatch cost should dominate. If
              # block time barely moves at 8 ranks, then the 2.65 s per forward is
              # overhead and halving the ranks roughly doubles fps; if block time
              # doubles, the work is compute-bound after all and 16 is right.
              # 1200 divides both 8 and 16, so the width does not have to change.
              echo "============================================"
              echo "  Self-Forcing T2V on trn2: ${NPROC} ranks TP=4, ${NUM_OUTPUT_FRAMES} latent frames"
              echo "============================================"

              torchrun --nproc_per_node ${NPROC} neuron/sf_inference.py \
                --prompt_file "${PROMPT_FILE}" \
                --config_path "${CONFIG_PATH}" \
                --checkpoint_path checkpoints/self_forcing_dmd.pt \
                --output_folder videos_sf \
                --num_output_frames "${NUM_OUTPUT_FRAMES}" \
                --tp_degree 4 \
                --latent_h 60 \
                --latent_w "${LATENT_W}" \
                --fps "${FPS}" \
                --max_prompts "${MAX_PROMPTS}" \
                --use_ema

              # ─── Archive to PVC ──────────────────────────────────────────
              TIMESTAMP=$(date +%Y%m%d_%H%M%S)
              ARCHIVE_DIR="/var/mdl/self_forcing/runs/${TIMESTAMP}"
              echo "============================================"
              echo "  ARCHIVING to $ARCHIVE_DIR"
              echo "============================================"
              mkdir -p "$ARCHIVE_DIR"
              cp videos_sf/*.mp4 "$ARCHIVE_DIR/" 2>/dev/null || true
              # Per-frame PNGs, so the frames can be inspected without decoding the mp4.
              if [[ -d videos_sf/frames ]]; then
                cp -r videos_sf/frames "$ARCHIVE_DIR/frames"
              fi
              git -C /tmp/Self-Forcing rev-parse HEAD > "$ARCHIVE_DIR/self_forcing.sha"
              git -C /tmp/rolling_forcing rev-parse HEAD > "$ARCHIVE_DIR/rolling_forcing.sha"
              echo "--- archive contents ---"
              ls -la "$ARCHIVE_DIR/"
              find "$ARCHIVE_DIR/frames" -name '*.png' 2>/dev/null | wc -l | xargs echo "PNG frames archived:"
              du -sh "$ARCHIVE_DIR"

              echo "═══════════════════════════════════════════"
              echo "  Done! Self-Forcing inference complete"
              echo "═══════════════════════════════════════════"
          resources:
            claims:
              - name: m-trn2
            # Exactly rolling-forcing-job.yaml's trn2 footprint. No ephemeral-storage
            # request, matching theirs: one node in this cluster has only ~460GiB and a
            # large request goes Pending. SF_LOAD_WAVES keeps the checkpoint-load peak
            # well inside this.
            requests:
              cpu: 24
              memory: 256Gi
            limits:
              cpu: 24
              memory: 256Gi
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm
            - name: 621547421844-ap-southeast-4-pvc
              mountPath: /var/mdl
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
            # So the log names the node it ran on: the same config succeeded on one node
            # and fails on another.
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            # Which code to run. SF_BRANCH is the branch of yahavb/Self-Forcing
            # holding neuron/; RF_COMMIT pins the Neuron stack.
            - name: SF_BRANCH
              value: "neuron-trn"
            - name: RF_COMMIT
              value: "0cfc734656aa0ff72438f4ba2771056f4df52807"
            # Run shape. 21 latent frames -> 81 pixel frames; the checkpoint was
            # distilled at 21 and asserts if pushed past the attention window.
            # BRING-UP: 6 (2 blocks) while chasing the non-finite pixels — it
            # exercises block 0 (unroped anchor) and block 1 (anchor re-RoPE +
            # carry) at a third of the device time. Restore 21 once clean.
            # Rank count. 16 is the measured-good value (0.78 fps, run 20260907_045344)
            # and mirrors rolling_forcing. 8 is dimensionally valid (1200 % 8 == 0) but
            # hung: 7 ranks parked on one collective with escalating 120/240/480/960 s
            # retries while one rank was elsewhere. Reading the SP=2 path found no
            # rank-divergent collective -- q_local L/world=450 all-gathered over 4 tp
            # ranks gives 1800 = L/sp, consistent; the KV buffer still rounds to 32768.
            # What does change is that every graph shape is new at 8 ranks, so one
            # straggler in a cold compile parks the rest. Unexplained, so not used.
            # Load the 11.4GB T5 and 5.7GB DiT checkpoints in this many waves, so peak
            # host RAM is world/waves instead of world x 17GB.
            - name: SF_LOAD_WAVES
              value: "4"
            - name: NPROC
              value: "8"
            - name: NUM_OUTPUT_FRAMES
              value: "21"
            # Per-call finiteness/magnitude probes, and don't abort on non-finite
            # pixels so one run localizes the problem. Set to "0" for real runs.
            # MUST be 0 for any run whose fps is quoted. The probes sync the device inside
            # the DiT phase -- clean_latent plus three KV-cache probes over millions of
            # elements per block, all counted in dit_ms -- so they report worse than the
            # truth. Correctness was already established with them on: the 20260907_023131
            # run was clean end to end.
            - name: SF_DEBUG_NUMERICS
              value: "0"
            - name: FPS
              value: "16"
            - name: PROMPT_FILE
              value: "prompts/MovieGenVideoBench_extended.txt"
            # 2, for measurement: prompt 0 pays to trace one flash-attention
            # specialisation per distinct attention-window length (7 at 21 frames),
            # prompt 1 reuses them in-process. Prompt 1 is the honest fps.
            - name: MAX_PROMPTS
              value: "3"
            # ─── Neuron runtime (mirrors rolling_forcing's inference jobs).
            # No NEURON_LOGICAL_NC_CONFIG: the resource claim sets LNC.
            - name: NEURON_RT_LOG_LEVEL
              value: "ERROR"
            - name: NEURON_CC_LOG_LEVEL
              value: "ERROR"
            - name: TORCH_NEURONX_LOG_LEVEL
              value: "ERROR"
            - name: NEURON_FALLBACK_ENABLED
              value: "0"
            - name: NEURON_CC_FLAGS
              value: "--model-type=transformer"
            - name: NEURON_RT_ASYNC_EXEC_MAX_INFLIGHT_REQUESTS
              value: "0"
            - name: NEURON_RT_DBG_INTRA_RDH_CHANNEL_BUFFER_SIZE
              value: "134217728"
            - name: USE_NKI_KERNELS
              value: "true"
            # The SDK warns this cache "can yield stale cache hits". With 8 ranks
            # sharing it and per-rank-different VAE width shards, a cross-rank hit
            # is a plausible source of structurally-corrupt output. Off while the
            # non-finite pixels are unexplained; costs compile time only.
            # 1 to cut prompt 0's tracing (~44-60 s vs ~350-450 s per block). It does not
            # touch steady-state execution. Every failure once blamed on it was on the
            # unsharded path, which is gone; the sharded path has run clean with it both on
            # (runs 1-2) and off (20260907_023131).
            - name: NKI_ENABLE_TRACE_CACHE
              value: "1"
            # bf16 | fp32. fp32 is the control for "is this decoder overflow?"
            # -- ruled out: the NaNs are all NaN, zero Inf.
            - name: SF_VAE_DTYPE
              value: "bf16"
            # world | none. "none" gives every rank a singleton vae-sp group, so
            # models/vae.py takes its world == 1 fallbacks: plain PyTorch conv, no
            # halo exchange, no extract_w_edges/restore_layout NKI kernels. Each
            # rank decodes the full width redundantly. This is both the bisection
            # for the NaN band and a working fallback if the sharded path is broken.
            - name: SF_VAE_SHARD
              value: "world"
            # One PNG per frame in videos_sf/frames/prompt_NNN/, archived to the PVC
            # alongside the mp4.
            - name: SF_SAVE_FRAMES
              value: "1"
            # Safety only: the default 30-minute collective timeout is shorter than a
            # cold compile, and when it fires the runtime retries with exponential
            # backoff instead of failing -- that is how one run burned four hours.
            # Changes nothing on a healthy run.
            - name: SF_COLLECTIVE_TIMEOUT_S
              value: "7200"
            # CONFIG_PATH and LATENT_W must agree: fs1560 <-> 104 (480x832, native),
            # fs1200 <-> 80 (480x640, the width every RF trn3 job runs).
            - name: CONFIG_PATH
              value: "neuron/configs/self_forcing_dmd.yaml"
            - name: LATENT_W
              value: "104"
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hf-token
                  key: HF_TOKEN
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github-token
                  key: GITHUB_TOKEN
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 32Gi
        - name: 621547421844-ap-southeast-4-pvc
          persistentVolumeClaim:
            claimName: 621547421844-ap-southeast-4-pvc
